// let p = document.querySelector("#son");
// let header = document.querySelector(".nav-section");

// let headerObserver = new IntersectionObserver(
//   (entries) => {
//     const [watch] = entries;
//     if (watch.isIntersecting) {
//       header.classList.add("sticky-header");
//     } else {
//       header.classList.remove("sticky-header");
//     }
//   },
//   { threshold: 1 }
// );
// headerObserver.observe(p);
